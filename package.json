{
  "name": "my-call-time",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal timezone converter for international calls.",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "check": "node --check dist/core.js && node --check dist/app.js"
  },
  "engines": { "node": ">=20" }
}
